@model List<Lib.Data.ContaDal>


<h3>Painel Admin</h3>

<hr />
<div class="row">
    <div class="col-3">
        <a asp-action="Cargos" asp-controller="PainelAdmin" class="btn btn-primary">Editar Cargos</a>
    </div>
</div>
<hr />

<form method="post">
    <div class="row">
        <div class="col-11">
            <input id="Pesquisa" placeholder="Digite uma palavra relacionada a conta." name="NomeDaTarefa" class="form-control" />
        </div>
        <div class="col-1">
            <button type="submit"><i class="fa-solid fa-magnifying-glass"></i></button>
        </div>
    </div>
    <div>
        <table class="table mt-3">
            <thead>
                <tr>
                    <th>Usuario</th>
                    <th>Email</th>
                    <th>Cargo</th>
                </tr>
            </thead>
            <tbody>
                @{

                    if (Model != null)
                        foreach (var conta in Model)
                        {
                            <tr>
                                <td width="20%">@conta.Conta</td>
                                <td width="20%">@conta.Email</td>
                                <td width="20%">@(conta.Admin?"Sim":"Não")</td>
                            </tr>
                        }
                }
            </tbody>
        </table>
    </div>
</form>