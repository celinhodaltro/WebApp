<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.7/css/all.css">
    <link rel="stylesheet" href="~/css/fontawesome-free-6.1.1-web/css/all.css">
</head>
<body>
    <header>

        @{
            var pagina = ViewData["Entrar"];
        }
        @if (pagina == null)
        {
            <nav class="sidebar">
                <div class="text"> Organize </div>
                <ul class="main_side">
                    <li class="active"><a href="#">Dashboard</a></li>
                    <li>
                        <a href="#" id="1">Planejamentos <span class="fas fa-caret-down"></span> </a>
                        <ul class="item-show-1">
                            <li><a asp-action="Tarefas" asp-controller="Planejamento"><i class="fa-solid fa-calendar mr-2"></i>Tarefas</a></li>
                            <li><a asp-action="Economias" asp-controller="Planejamento"><i class="fa-solid fa-sack-dollar mr-2"></i>Economias</a></li>
                        </ul>
                    </li>
                </ul>
            </nav>
            <div class="content ">
                <div class="header">
                    @RenderBody()
                </div>
            </div>
        }
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @if (ViewData["Entrar"] != null)
            {
                @RenderBody()
            }
        </main>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>

<script>

    $('.sidebar').toggleClass("show");


    $('.sidebar ul li a').click(function () {
        var id = $(this).attr('id');
        $('nav ul li ul.item-show-' + id).toggleClass("show");
        $('nav ul li #' + id + ' span').toggleClass("rotate");

    });

    $('nav ul li').click(function () {
        $(this).addClass("active").siblings().removeClass("active");
    });
</script>
